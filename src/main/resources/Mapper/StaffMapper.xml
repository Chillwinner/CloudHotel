<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aura.hotel.Mapper.StaffMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO staff (name, phone, gender, hotel_id, role, status)
        VALUES (#{name}, #{phone}, #{gender}, #{hotelId}, #{role}, #{status})
    </insert>

    <delete id="delete">
        DELETE FROM staff WHERE id = #{id}
    </delete>

    <update id="update">
        UPDATE staff
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="hotelId != null">hotel_id = #{hotelId},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" resultType="com.aura.hotel.entity.Staff">
        SELECT id, name, phone, gender, hotel_id, role, status, create_time
        FROM staff
        WHERE id = #{id}
    </select>

    <select id="selectList" resultType="com.aura.hotel.entity.Staff">
        SELECT id, name, phone, gender, hotel_id, role, status, create_time
        FROM staff
        <where>
            <if test="hotelId != null">
                AND hotel_id = #{hotelId}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>