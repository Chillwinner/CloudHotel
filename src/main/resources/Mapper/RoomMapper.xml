<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aura.hotel.Mapper.RoomMapper">

    <insert id="insertRoom" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO room (hotel_id, room_number, type_name, price, status)
        VALUES (#{hotelId}, #{roomNumber}, #{typeName}, #{price}, #{status})
    </insert>

    <update id="updateRoom">
        UPDATE room
        <set>
            <if test="roomNumber != null">room_number = #{roomNumber},</if>
            <if test="typeName != null">type_name = #{typeName},</if>
            <if test="price != null">price = #{price},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteRoom">
        DELETE FROM room WHERE id = #{id}
    </delete>

    <select id="selectById" resultType="com.aura.hotel.entity.Room">
        SELECT * FROM room WHERE id = #{id}
    </select>

    <select id="selectByHotel" resultType="com.aura.hotel.entity.Room">
        SELECT * FROM room
        WHERE hotel_id = #{hotelId}
        <if test="typeName != null and typeName != ''">
            AND type_name = #{typeName}
        </if>
        ORDER BY room_number ASC
    </select>

    <select id="selectTypeNamesByHotel" resultType="string">
        SELECT DISTINCT type_name FROM room WHERE hotel_id = #{hotelId}
    </select>

</mapper>