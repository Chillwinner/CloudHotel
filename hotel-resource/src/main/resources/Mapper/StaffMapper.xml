<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Aura.Mapper.StaffMapper">

    <!-- 新增：根据手机号查询 -->
    <select id="selectByPhone" resultType="com.Aura.entity.Staff">
        SELECT * FROM staff WHERE phone = #{phone} AND status = 1
    </select>

    <!-- 修改 insert，加上 password -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO staff (name, phone, password, gender, hotel_id, role, status)
        VALUES (#{name}, #{phone}, #{password}, #{gender}, #{hotelId}, #{role}, #{status})
    </insert>

    <!-- 修改 update，加上 password -->
    <update id="update">
        UPDATE staff
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="password != null">password = #{password},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="hotelId != null">hotel_id = #{hotelId},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM staff WHERE id = #{id}
    </delete>



    <select id="selectById" resultType="com.Aura.entity.Staff">
        SELECT id, name, phone, gender, hotel_id, role, status, create_time
        FROM staff
        WHERE id = #{id}
    </select>

    <select id="selectList" resultType="com.Aura.entity.Staff">
        SELECT id, name, phone, gender, hotel_id, role, status, create_time
        FROM staff
        <where>
            <if test="hotelId != null">
                AND hotel_id = #{hotelId}
            </if>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>